"use strict"; // treat all js code with new version

alert(3+3) //not allowed this in node.js with this syntax of alert; allowed in browser.

/*
datatypes:

1.number
2.bigint
3.string ""
4.null => stand alone value or empty
5.undefined => value not assigned
6.symbol => for uniqueness

objects:

** null is an object but undefined is itself a datatype that is  undefined, it is not an object
  console.log(typeof variable_name)
  */
